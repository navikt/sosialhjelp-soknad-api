apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: sosialhjelp-soknad-api-test
  namespace: default
  labels:
    team: digisos
spec:
  image: docker.adeo.no:5000/sosialhjelp-soknad-api-test:1.21
  port: 8080
  ingresses:
  - "https://sosialhjelp-test.nais.io/soknad-api"
  - "https://sosialhjelp-api-test.dev-sbs.nais.io"
  prometheus:
    enabled: true
    path: /prometheus
  liveness:
    path: /soknadsosialhjelp-server/internal/isAlive
    initialDelay: 60
    timeout: 1
    periodSeconds: 10
    failureThreshold: 10
  readiness:
    path: /soknadsosialhjelp-server/internal/isAlive
    initialDelay: 60
    timeout: 1
  resources:
    limits:
      cpu: "1000m"
      memory: 512Mi
    requests:
      memory: 512Mi
  replicas:
    min: 1
    max: 1
  env:
  - name: dockerWithDefaultMockActivated
    value: "true"
