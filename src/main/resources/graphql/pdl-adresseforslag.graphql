query($variables: CompletionParameters) {
    forslagAdresse(parameters: $variables) {
        suggestions
        addressFound {
            vegadresse {
                matrikkelId
                adressenavn
                husnummer
                husbokstav
                postnummer
                poststed
                kommunenavn
                kommunenummer
                bydelsnavn
                bydelsnummer
            }
        }
    }
}
