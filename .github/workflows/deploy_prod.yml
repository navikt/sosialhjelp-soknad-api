name: Deploy to Production
on:
  workflow_run:
    workflows: [ 'Build Kotlin, Run Lint and Test' ]    # Venter på at bygg på master har kjørt uten feil
    branches: [ master ]
    paths-ignore:
      - '*.md' # Ignorerer endringer i markdown-filer
      - 'compose/**' # Ignorerer endringer i docker-compose-filer
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy-to-prod:
    name: 'Deploy to prod-fss'
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    permissions:
      id-token: write
      contents: write
    uses: navikt/sosialhjelp-ci/.github/workflows/deploy_app.yml@v4
    with:
      cluster-name: 'prod-fss'
      resource-folder: 'prod'
      resource-name: 'prod-fss'
    secrets: inherit
